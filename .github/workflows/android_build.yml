name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build with Gradle
        run: |
          # 1. إعداد الذاكرة لمنع خطأ OutOfMemory
          echo "org.gradle.jvmargs=-Xmx6g -XX:MaxMetaspaceSize=2g" > gradle.properties
          
          # 2. الانتقال إلى المجلد الذي يحتوي على ملف build.gradle.kts الخاص بالتطبيق
          cd LandscapeDesignApp/LandscapeDesignApp
          
          # 3. إصلاح مشكلة الأيقونة المفقودة عبر التعديل النصي (sed)
          # نستخدم المسار النسبي من داخل مجلد المشروع
          sed -i 's|android:icon="@mipmap/ic_launcher"|android:icon="@android:drawable/sym_def_app_icon"|g' app/src/main/AndroidManifest.xml
          
          # 4. تنظيف المشروع والبناء باستخدام Gradle العام
          # تم استبعاد test و lint لتوفير الذاكرة وسرعة البناء
          gradle clean assembleDebug -Pandroid.useAndroidX=true -Pandroid.enableJetifier=true -x lint -x test

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LandscapeDesignApp
          path: LandscapeDesignApp/LandscapeDesignApp/app/build/outputs/apk/debug/app-debug.apk
